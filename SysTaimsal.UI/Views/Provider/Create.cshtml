@model SysTaimsal.EL.Provider

@{
    ViewData["Title"] = "Create";
}

<h1>Añadir Proveedor</h1>

<h4>Proveedor</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group mb-3">
                <label asp-for="NameProvider" class="control-label">Nombre de Proveedor</label>
                <input asp-for="NameProvider" class="form-control" />
                <span asp-validation-for="NameProvider" class="text-danger"></span>
            </div>
            <div class="form-group mb-3">
                <button type="submit" value="Create" class="btn btn-success">Crear</button> |
                <a type="button" asp-action="Index" class="btn btn-primary">Volver a la Lista</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
